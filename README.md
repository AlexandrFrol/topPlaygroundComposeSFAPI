# Space Flight News - Android приложение

Android приложение для просмотра новостей о космических полетах, построенное с использованием Jetpack Compose и современных архитектурных паттернов.

## 📱 Описание

Приложение предоставляет возможность просматривать актуальные новости о космических полетах из [Space Flight News API](https://api.spaceflightnewsapi.net/). Пользователи могут просматривать список статей, читать детальную информацию о каждой статье, добавлять статьи в избранное и открывать полные версии статей в браузере.

## 🏗️ Архитектура

Проект построен на основе **Clean Architecture** с разделением на три основных слоя:

- **Presentation** - UI слой с использованием Jetpack Compose и ViewModel
- **Domain** - бизнес-логика, модели и use cases
- **Data** - источники данных (Remote API и Local Database)

## 🛠️ Технологический стек

### Основные технологии
- **Kotlin** - основной язык разработки
- **Jetpack Compose** - современный UI фреймворк
- **Material Design 3** - дизайн-система

### Архитектурные компоненты
- **Hilt** - dependency injection
- **ViewModel** - управление состоянием UI
- **Navigation Compose** - навигация между экранами

### Сетевой слой
- **Retrofit** - HTTP клиент для работы с API
- **OkHttp** - HTTP клиент с логированием запросов
- **Gson** - сериализация/десериализация JSON

### Локальное хранилище
- **Room** - база данных для хранения избранных статей
- **Coroutines** - асинхронное программирование

### Дополнительные библиотеки
- **Coil** - загрузка и кэширование изображений

## 📋 Функциональность

- ✅ Просмотр списка новостей о космических полетах
- ✅ Детальный просмотр статьи с изображением и описанием
- ✅ Добавление/удаление статей в избранное
- ✅ Просмотр списка избранных статей
- ✅ Открытие полной версии статьи в WebView
- ✅ Навигация между экранами с сохранением состояния

## 📦 Структура проекта

```
app/src/main/java/com/example/topplaygroundcompose/
├── data/                    # Слой данных
│   ├── local/              # Локальное хранилище (Room)
│   │   ├── dao/            # Data Access Objects
│   │   ├── entity/         # Entity классы
│   │   └── SpaceFlightNewsDatabase.kt
│   ├── remote/             # Удаленный API
│   │   ├── dto/            # Data Transfer Objects
│   │   └── SpaceFlightNewsApi.kt
│   └── repository/         # Реализация репозиториев
├── domain/                  # Слой бизнес-логики
│   ├── model/              # Доменные модели
│   ├── repository/         # Интерфейсы репозиториев
│   └── usecase/            # Use cases
├── presentation/            # UI слой
│   ├── articles/           # Экран списка статей
│   ├── article_detail/     # Экран деталей статьи
│   ├── article_webview/    # WebView для полной статьи
│   ├── favorites/          # Экран избранного
│   ├── components/         # Переиспользуемые компоненты
│   ├── navigation/        # Навигация
│   └── MainScreen.kt       # Главный экран
├── di/                      # Dependency Injection модули
├── ui/                      # UI тема и стили
├── MainActivity.kt
└── SpaceFlightNewsApplication.kt
```

## 🚀 Начало работы

### Требования

- Android Studio Hedgehog (2023.1.1) или новее
- JDK 11 или выше
- Android SDK 26 (Android 8.0) или выше
- Gradle 8.13.1

### Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd topPlaygroundComposeSFAPI
```

2. Откройте проект в Android Studio

3. Дождитесь синхронизации Gradle (загрузка зависимостей)

4. Запустите приложение на эмуляторе или физическом устройстве

### Сборка проекта

Для сборки debug версии:
```bash
./gradlew assembleDebug
```

Для сборки release версии:
```bash
./gradlew assembleRelease
```


## 🔧 Конфигурация

### Версии

- **Min SDK**: 26 (Android 8.0)
- **Target SDK**: 36
- **Compile SDK**: 36
- **Kotlin**: 2.0.21
- **Gradle**: 8.13.1

### API

Приложение использует публичный API [Space Flight News API](https://api.spaceflightnewsapi.net/), который не требует аутентификации.

